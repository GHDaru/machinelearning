# Dependências da trilha prática. Mínimas e fixadas por faixa maior —
# anti-apodrecimento (regra 3 da construção).
#
# A etapa 00 roda com ZERO dependências além do pytest: só biblioteca padrão.
# As demais entram quando a etapa correspondente as exigir, e não antes.

pytest>=8.0

# etapa 05 em diante — álgebra linear
numpy>=2.0

# etapa 15 em diante — o modelo vira serviço
fastapi>=0.115
uvicorn[standard]>=0.30

# etapa 21 — exploração: ler distribuição, não desenhar gráfico (ADR 0010).
# É a ÚNICA etapa que sai da biblioteca padrão. Já vêm no Colab, então a
# trilha continua rodando sem instalar nada lá.
pandas>=2.2
matplotlib>=3.8

# etapa 19 — o MLP em dados reais (capítulo III.2).
#
# É a PRIMEIRA biblioteca de modelo da trilha, e ela entrou fora da ordem que a
# Restrição 1 pede (NumPy do zero antes da chamada de uma linha). A antecipação
# foi decisão do autor, para a turma poder rodar já — e a dívida foi PAGA: a
# rede densa escrita à mão está em etapa-19/rede.py, com conferência de
# gradiente contra diferença finita, e chega a MAE 0,3990 contra 0,3878 da
# biblioteca no mesmo recorte.
#
# O dataset NÃO é baixado em tempo de execução: ele está congelado em
# ml-zero/dados/california/, com sha256 na ficha. O sklearn aqui é o modelo, e
# não a porta de entrada para a rede.
scikit-learn>=1.5
